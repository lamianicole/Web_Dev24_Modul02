const ordersDiv = document.getElementById('orders')!;

document.getElementById('startSimulation')?.addEventListener('click', () => {

})
// order ist nichts anderes als ein div, s. html

function createCustomerOrder(customerNumber: number, drink: string): void {

}

// oft Konflikte: id ist number, dann Konflikt mit string

// vom Promise.all kommt alles als string zurück