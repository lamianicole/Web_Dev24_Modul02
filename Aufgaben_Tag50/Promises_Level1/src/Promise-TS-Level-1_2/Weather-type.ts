enum WeatherType {
    none = 0,
    sunny = 1,
    cloudy = 2,
    rainy = 3,
    snowy = 4,
    stormy = 5,
    windy = 6
}

// Nummern wären im Grunde nicht notwendig, weil bei enums ohnehin über index gearbeitet wird

export default WeatherType;
